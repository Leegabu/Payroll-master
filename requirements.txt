# Payroll System - Installation Requirements

# --- Backend Requirements ---
# (Used if you run backend scripts with Python)
python>=3.10
mysql-connector-python
flask
flask-cors
requests
pandas

# --- Frontend Tools ---
# These are managed via package.json (Node.js + npm)
# Node.js >= 18
# npm >= 9
# Key packages (already installed via npm):
# @mui/material, @emotion/react, @tanstack/react-query, react-router-dom, axios

# --- Environment Setup ---
# Copy .env.example to .env and update credentials (MySQL, API URL, etc.)
# Then run the following:
# npm install
# npm run dev

# --- Notes ---
# Each developer should work in their own branch (e.g., kelvin-frontend)
# and submit Pull Requests for review before merging to main.
